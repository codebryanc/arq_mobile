class Actor {
  // [Constructor]
  const Actor({
    required this.id,
    required this.name,
    required this.character,
    required this.profilePath,
  });

  // [Properties]
  final int id;
  final String name;
  final String character;
  final String profilePath;
}
